---
import DocsLayout from '../../../layouts/DocsLayout.astro';
---

<DocsLayout title="快速开始" description="几分钟内让 PicFast 运行起来。">
  <h1>快速开始</h1>
  <p>
    PicFast 是一个自托管的图片托管服务。不到 5 分钟即可运行。
  </p>

  <h2>环境要求</h2>
  <ul>
    <li><a href="https://docs.docker.com/get-docker/" target="_blank" rel="noopener">Docker</a> 和 Docker Compose</li>
    <li><a href="https://git-scm.com/" target="_blank" rel="noopener">Git</a></li>
  </ul>

  <h2>1. 克隆并配置</h2>
  <p>克隆仓库，复制配置文件：</p>
  <pre><code>git clone https://github.com/atbeta/picfast.git
cd picfast
cp .env.example .env
cp config.example.yaml config.yaml</code></pre>
  <p>编辑 <code>config.yaml</code>，至少设置一个强随机密钥：</p>
  <pre><code>jwt:
  secret: "your-strong-random-secret-here"</code></pre>

  <h2>2. 启动服务</h2>
  <p>一条命令同时启动 PostgreSQL、mailpit（本地邮件测试工具）和应用：</p>
  <pre><code>make docker-up</code></pre>
  <p>数据库迁移会在首次启动时自动执行。</p>

  <h2>3. 访问应用</h2>
  <p>应用运行在 <code>http://localhost:18080</code>。</p>
  <p>通过界面注册账号后，即可在后台管理中配置管理员权限。</p>

  <h2>接下来做什么？</h2>
  <ul>
    <li><a href="/zh/docs/configuration">配置参考 →</a></li>
    <li><a href="/zh/docs/storage">配置存储后端 →</a></li>
    <li><a href="/zh/docs/api">API 文档 →</a></li>
  </ul>
</DocsLayout>
