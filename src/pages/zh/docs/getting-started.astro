---
import DocsLayout from '../../../layouts/DocsLayout.astro';
---

<DocsLayout title="快速开始" description="数分钟内完成 PicFast 部署。">
  <h1>快速开始</h1>
  <p>
    PicFast 是自托管的图床平台，通过 Docker 数分钟内即可完成部署。
  </p>

  <h2>环境要求</h2>
  <ul>
    <li><a href="https://docs.docker.com/get-docker/" target="_blank" rel="noopener">Docker</a> 及 Docker Compose</li>
    <li><a href="https://git-scm.com/" target="_blank" rel="noopener">Git</a></li>
  </ul>

  <h2>1. 克隆并配置</h2>
  <p>克隆仓库，复制示例配置文件：</p>
  <pre><code>git clone https://github.com/atbeta/picfast.git
cd picfast
cp .env.example .env
cp config.example.yaml config.yaml</code></pre>
  <p>编辑 <code>config.yaml</code>，至少需要为 <code>jwt.secret</code> 设置一个强度足够的随机值：</p>
  <pre><code>jwt:
  secret: "your-strong-random-secret-here"</code></pre>

  <h2>2. 启动服务</h2>
  <p>一行命令同时启动 PostgreSQL、Mailpit（本地邮件测试）及应用：</p>
  <pre><code>make docker-up</code></pre>
  <p>数据库迁移会在首次启动时自动执行。若配置了 <code>admin_email</code> 与 <code>admin_password</code>，管理员账号也会自动创建。</p>

  <h2>3. 访问应用</h2>
  <p>打开 <code>http://localhost:18080</code>。注册新账号，或使用自动创建的管理员账号登录。</p>

  <h2>4. 上传第一张图片</h2>
  <p>你可以通过 Web UI、API、ShareX 或 MCP 服务器上传：</p>
  <pre><code># 通过 curl
curl -X POST http://localhost:18080/api/v1/images \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@image.png"

# 通过 MCP（AI Agent 集成）
npx @picfast/mcp</code></pre>

  <h2>备选方案：使用 Docker Hub 镜像</h2>
  <p>你也可以直接使用 Docker Hub 上发布的预构建镜像，无需克隆仓库：</p>
  <pre><code>docker network create picfast-net

docker run -d --name picfast-db --network picfast-net \
  -e POSTGRES_USER=picfast -e POSTGRES_PASSWORD=picfast \
  -e POSTGRES_DB=picfast \
  -v picfast-pgdata:/var/lib/postgresql/data \
  postgres:16-alpine

docker run -d --name picfast --network picfast-net -p 18080:8080 \
  -e PICFAST_DATABASE_URL='postgres://picfast:picfast@picfast-db:5432/picfast?sslmode=disable' \
  -e PICFAST_JWT_SECRET='replace-with-a-strong-secret' \
  -e PICFAST_SERVER_BASE_URL='http://localhost:18080' \
  -e PICFAST_APP_ADMIN_EMAIL='admin@example.com' \
  -e PICFAST_APP_ADMIN_PASSWORD='change-this-password' \
  -v picfast-uploads:/app/data/uploads \
  -v picfast-thumbnails:/app/data/thumbnails \
  xbeta/picfast:latest</code></pre>

  <h2>接下来</h2>
  <ul>
    <li><a href="/zh/docs/configuration">配置参考 &rarr;</a></li>
    <li><a href="/zh/docs/storage">配置存储后端 &rarr;</a></li>
    <li><a href="/zh/docs/api">API 文档 &rarr;</a></li>
    <li><a href="/zh/docs/sharex">配置 ShareX 集成 &rarr;</a></li>
    <li><a href="/zh/docs/deployment">生产环境部署 &rarr;</a></li>
  </ul>
</DocsLayout>
