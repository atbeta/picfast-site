---
import DocsLayout from '../../layouts/DocsLayout.astro';
---

<DocsLayout title="Quick Start" description="Get PicFast running in minutes.">
  <h1>Quick Start</h1>
  <p>
    PicFast is a self-hosted image hosting platform. It can be deployed via Docker in minutes.
  </p>

  <h2>Prerequisites</h2>
  <ul>
    <li><a href="https://docs.docker.com/get-docker/" target="_blank" rel="noopener">Docker</a> and Docker Compose</li>
    <li><a href="https://git-scm.com/" target="_blank" rel="noopener">Git</a></li>
  </ul>

  <h2>1. Clone and configure</h2>
  <p>Clone the repo and copy the example config files:</p>
  <pre><code>git clone https://github.com/atbeta/picfast.git
cd picfast
cp .env.example .env
cp config.example.yaml config.yaml</code></pre>
  <p>Edit <code>config.yaml</code> with your settings. At minimum, set <code>jwt.secret</code> to a strong random value:</p>
  <pre><code>jwt:
  secret: "your-strong-random-secret-here"</code></pre>

  <h2>2. Start everything</h2>
  <p>One command launches PostgreSQL, Mailpit (local email testing), and the app:</p>
  <pre><code>make docker-up</code></pre>
  <p>Database migrations run automatically on first startup. If you set <code>admin_email</code> and <code>admin_password</code>, an admin account is auto-created.</p>

  <h2>3. Access the app</h2>
  <p>Open <code>http://localhost:18080</code>. Register an account or log in if the admin account was auto-created.</p>

  <h2>4. Upload your first image</h2>
  <p>You can upload via the web UI, the API, ShareX, or through the MCP server for AI agents:</p>
  <pre><code># Via curl
curl -X POST http://localhost:18080/api/v1/images \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@image.png"

# Via MCP (AI agent integration)
npx @picfast/mcp</code></pre>

  <h2>Alternative: Docker Hub image</h2>
  <p>You can also run PicFast directly from the published Docker image without cloning the repo:</p>
  <pre><code>docker network create picfast-net

docker run -d --name picfast-db --network picfast-net \
  -e POSTGRES_USER=picfast -e POSTGRES_PASSWORD=picfast \
  -e POSTGRES_DB=picfast \
  -v picfast-pgdata:/var/lib/postgresql/data \
  postgres:16-alpine

docker run -d --name picfast --network picfast-net -p 18080:8080 \
  -e PICFAST_DATABASE_URL='postgres://picfast:picfast@picfast-db:5432/picfast?sslmode=disable' \
  -e PICFAST_JWT_SECRET='replace-with-a-strong-secret' \
  -e PICFAST_SERVER_BASE_URL='http://localhost:18080' \
  -e PICFAST_APP_ADMIN_EMAIL='admin@example.com' \
  -e PICFAST_APP_ADMIN_PASSWORD='change-this-password' \
  -v picfast-uploads:/app/data/uploads \
  -v picfast-thumbnails:/app/data/thumbnails \
  xbeta/picfast:latest</code></pre>

  <h2>What's next?</h2>
  <ul>
    <li><a href="/docs/configuration">Configuration reference &rarr;</a></li>
    <li><a href="/docs/storage">Set up storage backends &rarr;</a></li>
    <li><a href="/docs/api">API documentation &rarr;</a></li>
    <li><a href="/docs/sharex">Set up ShareX integration &rarr;</a></li>
    <li><a href="/docs/deployment">Production deployment &rarr;</a></li>
  </ul>
</DocsLayout>
